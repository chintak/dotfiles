# glow

Renders markdown in the terminal. Used for reading agent output and repo docs
without leaving the pane (`glow README.md`, or `yazi` → Enter on a `.md`).

`glow.yml` keys (`style`, `width`, `pager`) were taken from `glow --help`; if a
future release renames them, the file degrades to documentation and the env vars
still work.
