# zellij

An alternative multiplexer, kept configured but not the primary path. `zj`
(defined in `zshrc`) attaches to a session named after the current directory.
macOS-only in the `mac` profile because it's not needed on a headless box where
herdr + tmux already cover the work.
